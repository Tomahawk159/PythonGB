Задание на первый взгляд показалось сложным, потом разобрался.Вопросы котрые появились:

1. В задании написано, что функция должна вернуть значения, обязательно ли ретурнить в этих функциях,
   ведь на мой взгляд можно принтануть прямо в функции

2. Написанно что если не находит значения, то вернуть None, обязательно ли прописывать например else: return None
   если функция итак по умолчанию возвращает None?

3. Про Decimal почитал, я понял что для точности с числами и денежными операциями этот модуль лучше флоат,
   но в нашем случае мне показалось усложняет код и я использовал флоат для округления.

Распарсил как смог исходя из пройденых знаний, но почитал про библиотеки и парсинг в целом,
можно всё сделать эффективней и надёжней, но нужно ещё мне повникать в эту тему.